#!/bin/bash
cd "$(dirname "$0")/../"

source ./bin/begin.sh

python3 -c '
import asyncio
from backend.question_analyzer_FAISS_2 import full_pipeline, init_resources
import backend.create_FAISS_2
from utils.abbr_corrector_llm import process_abbreviations_with_llm

async def main():
    # backend.create_FAISS_2.init_resources()
    # backend.create_FAISS_2.build_or_update_index()
    await process_abbreviations_with_llm()

asyncio.run(main())
'